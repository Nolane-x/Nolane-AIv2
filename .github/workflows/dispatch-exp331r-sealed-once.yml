name: Dispatch sealed EXP-331R once

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/dispatch-exp331r-sealed-once.yml"

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch exact sealed EXP-331R branch
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh api
          --method POST
          repos/${{ github.repository }}/actions/workflows/exp331-pair-lattice-projection-safety.yml/dispatches
          -f ref=exp331r-pair-anchor-normalization
